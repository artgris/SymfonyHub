{% extends 'base.html.twig' %}

{% block content %}
    <div class="row text-center">
        <div class="col-lg-12">
            <p>{{ 'home_intro'|trans }}</p>
        </div>
        <div class="col-lg-12">
            <img class="rounded mx-auto d-block img-fluid" src="{{ asset('images/rambo.gif') }}" alt="">
        </div>
    </div>
{% endblock %}
